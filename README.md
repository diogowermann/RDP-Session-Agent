# RDP Session Agent

RDP Session Agent is a lightweight Windows collector for Remote Desktop Services session lifecycle telemetry.

The project is designed to work with RDP Session API while remaining infrastructure-agnostic. Environment-specific API URLs, server identifiers, credentials and deployment values must not be committed to this repository.

Development starts with a PowerShell-based Agent targeting Windows Server 2012 through Windows Server 2022.
